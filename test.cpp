#include <bits/stdc++.h>

using i64 = long long;

void solve() { 
    int n = 1;
    // std::cin >> n;
    std::cout << n << "\n";
    std::vector<int> a(n);
}

int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr), std::cout.tie(nullptr);

    int T = 1;
    while (T--) {
        solve();
    }

    return 0;
}